# 更新日志 Changelog

## v1.2.5 (2026-08-08)

### ✨ 新功能

- **配置与快捷键交互改版**
  - 每个配置右侧新增「应用开关」（n 选 1）：打开即应用该配置，其余配置开关自动关闭；未打开任何开关时改动进入临时配置，下次启动恢复默认。
  - 打开开关后，所有改动自动保存到该配置；首次改动时底部弹出「配置发生改变，是否保存？」保存条（蓝色保存 / 白色取消），而非静默自动保存。
  - 记忆各配置的开关状态（下次启动自动恢复）。
- **托盘菜单显示配置**
  - 右键系统托盘图标显示全部配置文件，当前生效的配置前打勾，点击即可一键切换。
- **配置管理增强**
  - 新增「导入配置 / 导出所选配置」（JSON 文件）。
  - 新增「重命名」，配置列表支持拖拽排序（顺序持久化，并同步修正按应用绑定与激活索引）。
- **全局开关快捷键**：配置入口移至头部「启用滤镜」开关下方；设置提示与配置快捷键提示分离，且 10 秒后自动消失。
- **主题复选框**不再随鼠标滚轮误切换。

### 🔧 修复

- 修复覆盖层重建后“可被 OBS 捕获”状态丢失，导致 WGC 录不到滤镜效果。
- 修复可被捕获时基于 DXGI 的截屏/录屏看不到 UI。
- 修复 HSL 分色系调色因逐像素透明（Premultiplied alpha）失效的问题（恢复不透明渲染）。

### 🎯 优化

- UI 置顶层级保持正确（滤镜层在 UI 之下、桌面之上），WGC 显示捕获可录到滤镜效果与 UI。

---

## v1.2.4 (2026-08-08)

### ✨ 新功能

- **全新 DXGI 逐像素着色器引擎**
  - 基于 Vortice.Direct3D11 的全 GPU 渲染管线（原始覆盖层窗口 + 交换链 + HSL 像素着色器 + DXGI Desktop Duplication 捕获），全程 GPU、无 CPU 往返。
  - 替代旧版的放大镜颜色矩阵引擎，实现真正的逐像素选择性 HSL 调整，且**不触发 Windows 11 屏幕捕获隐私边框**。
- **8 色系 HSL 分色系调色**
  - 红 / 橙 / 黄 / 绿 / 青 / 蓝 / 紫 / 品红 八个色系 + 全部（主），每个色系可独立精细调整**色相 / 饱和度 / 明度**，互不干扰（相对旧版 6 色系的近似模拟，现在是真·逐像素选择性调整）。
- **DXGI 引擎开关**
  - 可在「DXGI 逐像素引擎（支持 HSL）」与「放大镜 API 引擎」之间一键切换。
- **按应用切换滤镜改版**
  - 由单个进程升级为**多进程检测列表**，每个应用可**绑定一个配置文件**；某进程在前台时自动切换为绑定配置，列表内无进程在前台时自动关闭滤镜。
- **界面主题**
  - 新增「默认主题」与「Mica 毛玻璃主题」（Windows 11 风格标题栏），默认无毛玻璃以兼容老设备。
- **可被 OBS 捕获开关**
  - 开启后滤镜覆盖层与 UI 可被 OBS 等屏幕录制软件捕获，方便录屏保留滤镜效果。
- **调试日志系统**
  - 内置日志（%LocalAppData%\HScreenFilter\debug.log）+ 全局崩溃捕获，方便排查问题。

### 🔧 修复

- 修复关闭 DXGI 引擎时应用闪退（覆盖层 WM_DESTROY 不再投递 WM_QUIT）。
- 修复覆盖层全屏黑屏（D3D11 光栅化器背面剔除问题）。
- 修复画面上下颠倒（DXGI 纹理 UV 方向）。
- 修复调整 HSL 滑块时闪退（常量缓冲上传方式 + Dispose 竞态）。
- 修复反复开关 DXGI 后 HSL 失效（RegisterClassW 重复注册误判）。
- 修复新增标题栏行后内容与头部布局重叠。
- 解锁帧率（Present 无垂直同步）。

### 🎯 优化

- UI 窗口恒置顶，不再被滤镜覆盖层盖住。
- 鼠标指针区域不被滤镜着色 / 产生残影（光标排除，修正了索引对齐使其真正生效）。
- 移除 UI 置顶开关，避免误关后 UI 消失。

---

## v1.1.0

- 基于 WinUI 3 + 放大镜 API（MagSetFullscreenColorEffect）的屏幕滤镜工具。
- 五个核心滑块（亮度/对比度/鲜艳度/亮部/暗部）+ 色温 + 6 色系 HSL 近似。
- 配置文件、全局快捷键、按应用切换（单进程）、快捷预设、系统托盘、开机自启。
